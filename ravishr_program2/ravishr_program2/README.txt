The folder submitted, on canvas, is named ravishr_program2. Once you navigate into this folder: 

The files in this folder are:
	1. main.c : this is the file with the program code.
	2. movies_by_year : this is the executable.

The instructions to compile and run the program (main.c): 
	1. Open terminal and navigate into the ravishr_program2 directory.
	2. Compile main.c and generate executable file called movies_by_year by entering in terminal: 
		$ gcc —std=gnu99 -o movies_by_year main.c
	3. Run the executable and pass in the csv file by entering in the terminal:
		$ ./movies_by_year